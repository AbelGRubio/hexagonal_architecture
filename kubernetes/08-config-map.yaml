apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: event-architecture
data:
  REDIS_HOST: "redis"
  KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
  RABBITMQ_HOST: "rabbitmq"
  RABBITMQ_PORT: "5672"
